# Contains database connection information,
# API keys and other sensitive data to be
# separated from source controlled scripts.

db_defaults <- list(
  driver = "SQL Server",
  server = "DESKTOP-U2P5V4F",
  database = "eTBS_UTMA_Validation_V002a",
  username = "vbuser",
  password = "Th!nkvbuser"
)
